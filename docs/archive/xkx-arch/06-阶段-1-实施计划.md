# 阶段 -1 实施计划（第一垂直切片）

> 本文档是阶段 -1 垂直切片的实施计划，与 [04-迁移路径与避坑清单](04-迁移路径与避坑清单.md) 互补：04 是"阶段定义与门禁"，本文档是"第一切片怎么落地"。随切片迭代追加。
>
> **S1 已完成**（2026-07-10，30 tests 全绿 + ruff 全过）。**S2 已完成**（44 tests，[ADR-0003](../../docs/adr/ADR-0003-combatkernel-theme-neutrality.md)）。**S3 已完成**（52 tests，[ADR-0004](../../docs/adr/ADR-0004-agent-dsl-generation-s3.md)）。**S4a 已完成**（64 tests，[ADR-0005](../../docs/adr/ADR-0005-layer1-predicate-expansion.md)）。**S4b 已完成**（75 tests，[ADR-0006](../../docs/adr/ADR-0006-accept-object-inquiry-set-flag.md)）。**S4c 已完成**（83 tests，[ADR-0007](../../docs/adr/ADR-0007-minimal-quest-system.md)）/ **S4d 已完成**（92 tests，[ADR-0008](../../docs/adr/ADR-0008-schema-validator-four-checks.md)）/ **S4e 已完成**（99 tests，kill criteria 1 全量验证）。后续切片（S5 玩家试玩）追加于本文档。S1 简化项与补全时机见 [ADR-0002](../../docs/adr/ADR-0002-resolve-attack-extraction.md)。

---

## 一、第一切片 S1 的目标与范围

### 目标

端到端跑通阶段 -1 的两条核心链路，证明核心假设成立：

1. **DSL 表达力链路**：层0（房间/NPC 数据）+ 层1（事件规则）能定义一个最小可玩场景，编译到 JSON IR，被运行时加载。
2. **resolve_attack 纯函数链路**：从 LPC `do_attack` 七步提取出无副作用纯函数，seeded RNG 可重放，副作用按交织顺序记入账本。

两条链路在 stub 运行时里汇合：玩家能 `go` 移动（触发 `valid_leave` 规则）+ 能 `kill` NPC（触发 `resolve_attack` 跑一回合）。

### 范围（推荐 B）

| 维度 | S1 含 | S1 不含（后续切片） |
|---|---|---|
| resolve_attack | dodge/parry/hit 三分支 + 副作用账本 + seeded RNG + exp/damage effect | hit_ob/hit_by 的 mapping 分支、riposte 递归、武器类型完整集、技能 action 描述完整 |
| DSL 层0 | RoomDef + NpcDef 最小 schema + IR 编译 | 技能/物品/门 schema、字段穷举 |
| DSL 层1 | valid_leave 事件 + 条件阻断规则 + deny-wins | accept_object/chat_msg/init 等其他钩子、priority 复杂排序 |
| 运行时 | 最小 ECS（dict 存储）+ 6 组件 + go/kill 两命令 + 场景加载 | SparseSet、8 段管线、1s tick 调度、存储层、认证 |
| 场景 | 2 房间 + 1 战斗 NPC + 1 valid_leave 规则 | 5-10 房间、任务、对话、多 NPC |
| Agent | 不含（S3） | LLM 生成 DSL、copilot 修订量度量 |
| 非武侠验证 | 接口设计留口，不实跑（S2） | CombatKernel 非武侠微场景硬门禁 |
| 玩家试玩 | 不含（S5） | 3-5 名试玩 |

> **备选范围**：A（更小，仅 resolve_attack 最小版 dodge/hit + 层0 + 端到端，1-2 session，先证链路通）/ C（更大，B + hit_ob/hit_by + riposte + 非武侠，一次到位但风险高）。推荐 B：是一个有意义的完整闭环，验证阶段 -1 两条核心链路，又不致过大。

---

## 二、交付物与目录结构

```text
engine/src/xkx/
├── combat/
│   ├── context.py         # CombatContext 快照（attacker/victim 组件快照 + seed + weapon）
│   ├── result.py          # CombatRoundResult + Effect 账本（按交织顺序）
│   ├── rng.py             # seeded DeterministicRNG（combat 范围）
│   └── resolve_attack.py  # 纯函数七步管线（从 combatd.c do_attack 提取）
├── dsl/
│   ├── layer0.py          # RoomDef/NpcDef pydantic v2 模型 + YAML 加载
│   ├── layer1.py          # EventRule + 薄求值器（不命名"引擎"）
│   └── ir.py              # 编译层0/层1 到 JSON IR
├── runtime/
│   ├── components.py      # Identity/Position/Vitals/Attributes/CombatState/Skills
│   ├── ecs.py             # 最小 ECS（dict 存储，SparseSet 后置）
│   ├── commands.py        # go（移动+valid_leave）/ kill（调 resolve_attack）命令
│   └── world.py           # 加载 IR -> 构建实体 + 执行命令
engine/scenes/wuxia_micro/
├── rooms.yaml             # 2 房间（仿 chaguan 茶馆 + 相邻街道）
├── npcs.yaml              # 1 战斗 NPC（仿 bing 官兵）
└── rules.yaml             # 1 valid_leave 条件阻断规则（仿 yamen/duchang）
engine/tests/
├── test_resolve_attack.py # hypothesis 确定性重放
├── test_dsl_compile.py    # YAML -> IR 校验
├── test_event_rule.py     # valid_leave 求值 + deny-wins
└── test_e2e.py            # 加载场景 -> go -> kill -> 一回合 -> 断言
```

---

## 三、实施步骤

1. **resolve_attack 纯函数 + seeded RNG**（最核心最难，先做）
   - `CombatContext` / `CombatRoundResult` / `Effect` 数据模型（pydantic 或 dataclass）
   - `DeterministicRNG`（注入 `random.Random(seed)`，封装 do_attack 的 18 处 random 用途）
   - `resolve_attack(ctx) -> CombatRoundResult`：七步提取，dodge/parry/hit 三分支；hit_ob/hit_by 先做 int 加成/覆盖（mapping 分支后置）；riposte 标记不递归
   - 副作用全部产出为 `Effect`（DamageEffect/ExpEffect/JingliEffect），按交织顺序入账本，**不直接 mutate**
   - hypothesis 测试：同 seed + 同输入 -> 同输出

2. **层0 schema + IR 编译**
   - `RoomDef` / `NpcDef` pydantic v2 模型（字段参照 chaguan.c / bing.c）
   - YAML 加载（PyYAML）-> pydantic 校验 -> IR dict
   - 测试：加载 wuxia_micro YAML -> IR 无误

3. **层1 事件规则 + 薄求值器**
   - `EventRule`（event/condition/action/priority）
   - 最小谓词集：`attr_lt`（属性小于）/ `present_npc`（NPC 在场）/ `has_flag`（玩家标记）
   - 薄求值器：求 condition -> deny-wins 返回 allow/deny
   - 测试：yamen 型（守卫在场+无标记则 deny）能正确求值

4. **最小 ECS + 组件 + 场景加载**
   - 6 组件 dataclass；实体用 `entity_id -> {comp_type: comp}` dict（SparseSet 后置）
   - `world.load_ir(ir)`：RoomDef/NpcDef -> 实体 + 组件
   - 玩家实体 stub

5. **命令管线最小版**
   - `go(dir)`：查 exits -> 求 valid_leave 规则 -> deny 则阻止+消息，allow 则改 Position
   - `kill(target)`：进入战斗态 -> 调 `resolve_attack` -> apply effects 到组件 -> 产消息

6. **最小场景数据**
   - rooms.yaml：茶馆 + 街道，exits 互连
   - npcs.yaml：1 官兵（attitude heroism，可战斗）
   - rules.yaml：1 valid_leave 规则（如"未满 18 岁不得离开街道"，仿 duchang）

7. **端到端测试 + 修 bug**
   - 加载 wuxia_micro -> 玩家 go（触发 valid_leave）-> kill 官兵 -> resolve_attack 一回合 -> 断言伤害/消息/effect 顺序

8. **收尾**
   - 更新 [PROGRESS.md](../../PROGRESS.md)：S1 完成，标 S2 入手点
   - 写 ADR-0002：resolve_attack 提取决策（哪些步做了/简化了/为何，关联 05 dissent）

---

## 四、验收标准（对应阶段 -1 子集）

- [ ] `resolve_attack` 同 seed + 同输入确定性输出（hypothesis 通过）
- [ ] 副作用按"文本与状态交织真实顺序"记入账本，代码中无"先算后批量 apply"（测试断言 effect 顺序）
- [ ] 18 处 random 全部走注入 RNG，无裸 `random()`
- [ ] 层0 YAML 能定义 2 房间 + 1 NPC，编译 IR 无误
- [ ] 层1 valid_leave 能表达条件阻断规则，deny-wins 生效
- [ ] 端到端：go 移动 + valid_leave 阻断/放行 + kill 触发 resolve_attack 一回合 + 伤害/消息/effect 正确
- [ ] 代码不引入已砍复杂度（无分布式/PG/Redis/Rust/uvloop；combat 确定性 combat-only）
- [ ] ruff lint/format 全过，pytest 全绿

---

## 五、风险与对齐（关联 [05](05-第三轮专家对抗复审报告.md) dissent）

| 风险 | 对齐 | S1 应对 |
|---|---|---|
| CombatKernel 抽象时机张力（dissent 1） | 从武侠提取 vs 非武侠验证 | resolve_attack 接口不硬编码武侠语义（武器类型用枚举传入，"经脉/内力"等武侠概念不进核心签名），为 S2 非武侠验证留口；但 S1 不实跑非武侠 |
| 七步副作用交织（dissent / 01 子系统5） | 不得"先算后 apply" | Effect 账本严格按交织顺序，测试断言顺序 |
| 18 处 random（combat 确定性） | seeded RNG | 全部注入，hypothesis 验证可重放 |
| 层1 原语蠕变（dissent 3） | KPI <15% | S1 谓词集严格限定 3 个，扩充需 ADR |
| 阶段 -1 kill criteria 1（DSL+Agent 闭环） | DSL 表达力 | S1 先证层0+层1 能表达最小场景；若不能，触发扩原语（先于 Agent 投入） |

---

## 六、后续切片预告

- **S2**：非武侠微场景验证 CombatKernel 主题无关性（阶段 -1 硬门禁）✅ 已完成（见下节七 / [ADR-0003](../../docs/adr/ADR-0003-combatkernel-theme-neutrality.md)）
- **S3**：Agent 生成 DSL 初稿 + 人工修订量度量（copilot 模式）✅ 已完成（见下节八 / [ADR-0004](../../docs/adr/ADR-0004-agent-dsl-generation-s3.md)）
- **S4**：层1 谓词扩充 + accept_object/inquiry 对话 + 任务系统 + 扩展到 5-10 房间 + 2 NPC + 1 任务 + 1 对话全 DSL。S4a（层1 谓词扩充）✅ 已完成（见下节九 / [ADR-0005](../../docs/adr/ADR-0005-layer1-predicate-expansion.md)）；S4b（accept_object + inquiry）✅ 已完成（见下节十 / [ADR-0006](../../docs/adr/ADR-0006-accept-object-inquiry-set-flag.md)）；S4c（任务系统）✅ 已完成（见下节十一 / [ADR-0007](../../docs/adr/ADR-0007-minimal-quest-system.md)）；S4d（SchemaValidator 四道校验）✅ 已完成（[ADR-0008](../../docs/adr/ADR-0008-schema-validator-four-checks.md)）；S4e（扩展到 8 房间全量验证）✅ 已完成（kill criteria 1 全量）；剩余子任务（Agent 映射文档）待推进
- **S5**：3-5 名玩家试玩，"觉得好玩"达可继续投入阈值

---

## 七、S2 实施记录（非武侠微场景验证 CombatKernel 主题无关性）

> S2 完成（2026-07-10，44 tests 全绿 + ruff 全过）。本节记录 S2 的目标/重构/验证，详情见 [ADR-0003](../../docs/adr/ADR-0003-combatkernel-theme-neutrality.md)。

### 目标

兑现 [04](04-迁移路径与避坑清单.md) kill criteria 2 / 范围检查点 2：用非武侠微场景跑在 S1 的 resolve_attack + 运行时上，验证核心引擎未硬编码武侠语义。关联 [05](05-第三轮专家对抗复审报告.md) dissent 1（CombatKernel 抽象时机张力）。

### S1 留口审查发现（3 个武侠硬编码点）

1. `_select_attack_skill` 硬编码 `weapon in ("sword","blade")` -> 同名技能，非武侠武器 fallback 到 unarmed。
2. `_WEAPON_LABEL` 硬编码 sword->剑/blade->刀 渲染映射，非武侠武器 fallback "拳头"。
3. `neili`/`max_neili` 进了 `CombatantSnapshot` 核心签名（违反 ADR-0002 声明，resolve_attack 未用）。

判定逻辑层（dodge/parry/hit、skill_power、qi/jing/jingli、"dodge"/"parry" 技能名、limbs）审查结论主题无关，不动。

### 最小重构（映射外提到题材数据）

- `CombatantSnapshot`/`CombatState`/`NpcDef` 加 `attack_skill` + `weapon_label`，由题材数据声明。
- `resolve_attack` 删 `_select_attack_skill`/`_WEAPON_LABEL`，改读 `attacker.attack_skill`/`attacker.weapon_label`。
- `CombatantSnapshot` 删 `neili`/`max_neili`（`Vitals` 组件保留）；删 `WEAPON_SWORD`/`WEAPON_BLADE` 常量。

### 非武侠微场景

- 大航海（`scenes/age_of_sail_micro/`）：火枪（firearm）海盗，`attr_lt` 智力门槛规则。
- 书院（`scenes/academy_micro/`）：戒尺（ruler）监生，`present_npc` 看守规则（与武侠 `age_lt` / 大航海 `attr_lt` 形成谓词差异化）。

### 硬门禁自动化（`tests/test_theme_neutrality.py`）

- 非武侠 snapshot（firearm/ruler）跑 resolve_attack，断言消息含声明 weapon_label、skill_improve detail=声明 attack_skill。
- `inspect.getsource(resolve_attack)` 断言模块源码无 `"sword"`/`"blade"` 字面量（防回归硬门禁）。
- `CombatantSnapshot.model_fields` 断言不含 `neili`/`max_neili`。

### 范围边界（不做）

qi/jing/jingli 不重命名；action_damage_type/action_message 不外提（S4）；武器系统规格（阶段 0）；riposte 递归 / hit_ob-hit_by mapping（后续切片）。

---

## 八、S3 实施记录（Agent 生成 DSL 初稿 + 修订量度量）

> S3 完成（2026-07-10，52 tests 全绿 + ruff 全过）。本节记 S3 目标/方法/结果，详情见 [ADR-0004](../../docs/adr/ADR-0004-agent-dsl-generation-s3.md)。

### 目标

兑现 [04](04-迁移路径与避坑清单.md) kill criteria 1（DSL+Agent 创作闭环）+ 5（Agent 修订量）：Agent 从 LPC 规格生成 DSL 初稿，度量人工修订量，检测表达力缺口。关联 [05](05-第三轮专家对抗复审报告.md) §三 Q2 / §五 dissent 3（层1 原语护栏）。

### 方法

- copilot 近似：Agent = 本 session LLM，从 LPC 规格生成 v0 -> 专家修订 v1（M2 才接独立 LLM + Langfuse）
- 双载体：xueshan 大轮寺山门（守卫拦截 valid_leave）+ zhongnan 重阳宫大门（门状态机）
- 度量脚本 `tools/measure_revision.py`：四级校验 + 双比例 diff（含注释 vs 语义）+ GAP 台账

### 结果

| 场景 | 结构错误 | 语义修订 | 缺口 |
|---|---|---|---|
| xueshan | 0 | 28.3% | 4 |
| zhongnan | 0 | 20.7% | 6 |

- 语义修订 24.5% < 30% 降级线（kill criteria 5 未触发降级）
- 结构错误 0 = schema 弱校验信号（pydantic 类型 + 无引用完整性 + extra=ignore 静默忽略）
- Agent 典型偏差 3 类：neili/max_neili 混淆、map_skill 推断、武器 id vs 类别

### 表达力缺口台账（7 类，S4 统一评估）

family / has_item 谓词缺失、AND-OR 组合、allow-wins 语义、**方向绑定（e2e 发现，EventRule 无 dir 字段锁死场景，S4 最紧迫）**、accept_object 事件、门状态机。

### 范围边界（不做）

不接入 LLM API/Langfuse（M2）；不扩层1 谓词（S4）；不做任务/对话/accept_object schema（S4）；不做门状态机运行时（S4+）；不做 SchemaValidator 四道校验（S4/阶段 0）。

## 九、S4a 实施记录（层1 谓词扩充：方向绑定 + 组合 + family/has_item）

> S4a 完成（2026-07-10，64 tests 全绿 + ruff 全过）。本节记 S4a 目标/设计/结果，详情见 [ADR-0005](../../docs/adr/ADR-0005-layer1-predicate-expansion.md)。

### 目标

兑现 [ADR-0004](../../docs/adr/ADR-0004-agent-dsl-generation-s3.md) 表达力缺口台账中最紧迫项--**方向绑定**（EventRule 无 dir 字段，守卫规则锁死场景所有出口，S5 试玩无法进行），并扩充组合谓词 + family/has_item 覆盖 xueshan/zhongnan 完整 valid_leave 逻辑。关联 [05](05-第三轮专家对抗复审报告.md) §三 Q2 / §五 dissent 3（层1 原语护栏）。

### 设计（5 项扩充）

1. **方向绑定**：`EventRule.dir`（空=全方向向后兼容；非空=仅该方向匹配；对齐 LPC `valid_leave(me,dir)` 的 `if(dir=="north")`）。
2. **组合谓词**：`Predicate.all`(AND) / `any`(OR) / `not`(取反) 递归组合，任意布尔条件。
3. **`family_eq`**：actor 门派 == value（LPC `family/family_name`）。
4. **`has_item`**：actor 持有物品（LPC `present(obj, me)`）。
5. **allow-wins 不单独引入**：`not + deny-wins` 等价（zhongnan 全真教/持香放行 = `NOT(全真教 OR 持香) -> deny` 的反面）。

### LPC 规格对照

- `d/xueshan/shanmen.c`：`dir==north` + 葛伦布在场 + NOT(雪山派 OR 血刀门 OR 持酥油 OR 有标记) -> deny
- `d/zhongnan/gate.c`：`dir==north` + NOT(全真教 OR 持香) -> deny

两场景完整逻辑用扩充后谓词集表达，**无逃生舱**（KPI 达标，kill criteria 1 信号）。

### dissent 3 护栏

新增 1 规则维度（dir）+ 3 组合节点（all/any/not）+ 2 叶子谓词（family_eq/has_item），均有 LPC 规格源实证，非预判抽象。不引入规则引擎/RETE/OPA（Q2 否决）、独立 DSL 语法、allow-wins 独立语义。

### 结果

- 64 tests 全绿（+12：方向绑定 2 + 组合 5 + 嵌套 1 + e2e 放行 4），ruff 全过
- 方向绑定缺口解决：xueshan eastdown / zhongnan southdown 方向放行，S5 试玩路径打通
- 兑现 ADR-0004 缺口台账 5/7 类（剩余 2 类：accept_object 事件 / 门状态机，S4 剩余子任务）
- 度量脚本 L1-L4 全绿，结构错误 0

### 范围边界（不做）

不做 accept_object 事件 / 任务对话 schema / SchemaValidator 四道校验 / Agent 映射文档 / 门状态机运行时（S4 剩余子任务或阶段 0）；不修改 LPC 源。

## 十、S4b 实施记录（accept_object 事件 + inquiry 对话 + set_flag 副作用）

> S4b 完成（2026-07-10，75 tests 全绿 + ruff 全过）。本节记 S4b 目标/设计/结果，详情见 [ADR-0006](../../docs/adr/ADR-0006-accept-object-inquiry-set-flag.md)。

### 目标

兑现 [ADR-0004](../../docs/adr/ADR-0004-agent-dsl-generation-s3.md) 缺口台账 accept_object 项，覆盖 LPC NPC 级交互（`d/xueshan/npc/gelun1.c` 的 `accept_object` + `inquiry`）。为任务系统铺路（给物品完成任务）。关联 [05](05-第三轮专家对抗复审报告.md) §三 Q2 / §五 dissent 3。

### 设计（6 项扩充）

1. **inquiry 对话（layer0 数据）**：`NpcDef.inquiry: dict[str, str]`（topic -> reply）。`ask <npc> about <topic>` 查询。
2. **accept_object 事件（layer1 规则）**：`EventRule` 加 `npc_id` + `item_id` 绑定。首匹配求值（给物品是一次性事件）。
3. **set_flag 副作用 action**：`action="set_flag"` + `flag` 字段。设置 actor 标记（LPC `set_temp("marks/X")`）。
4. **Marks 组件**：存储玩家临时标记。补全 S4a 遗漏（`go` 未传 `actor_flags`，has_flag 谓词在 e2e 恒 False）。
5. **give 命令**：`give <npc> <item>` -> accept_object 规则 -> set_flag/deny + 物品移出。
6. **ask 命令**：`ask <npc> about <topic>` -> NPC inquiry -> reply。

### LPC 规格对照

- `d/xueshan/npc/gelun1.c::accept_object`：物品名=="酥油罐" -> `set_temp("marks/酥", 1)` + "佛爷保佑施主，里边请。"
- `d/xueshan/npc/gelun1.c::set("inquiry")`：ask about 还愿/烧香/供佛 -> `do_huanyuan` -> "你拿什麽孝敬佛爷呀？"

### 完整交互闭环（kill criteria 1 核心信号）

give 酥油罐 -> set marks/酥 -> 物品消耗后 go north 仍放行（has_flag 替代 has_item）。DSL 能表达"物品交互 -> 状态变更 -> 移动放行"闭环，**无逃生舱**。

### 结果

- 75 tests 全绿（+11：accept_object 求值 5 + xueshan e2e 6），ruff 全过
- 补全 S4a 遗漏：`go` 传 `actor_flags`，has_flag 谓词在 e2e 生效
- 兑现 ADR-0004 缺口台账 accept_object 项（剩余 1 类：门状态机，S4+ / 阶段 0）
- 度量脚本 L1-L4 全绿，结构错误 0

### 范围边界（不做）

不做任务 schema（需状态机/目标/奖励，S4 后续）；不做 SchemaValidator 四道校验 / Agent 映射文档（S4 后续）；不做门状态机运行时（S4+ / 阶段 0）；不修改 LPC 源。

## 十一、S4c 实施记录（最小任务系统）

> S4c 完成（2026-07-10，83 tests 全绿 + ruff 全过）。本节记 S4c 目标/设计/结果，详情见 [ADR-0007](../../docs/adr/ADR-0007-minimal-quest-system.md)。

### 目标

完成阶段 -1 kill criteria 1 的"1 任务 + 1 对话全 DSL"。对话在 S4b 落地；本切片覆盖任务系统。关联 [05](05-第三轮专家对抗复审报告.md) §三 Q2 / §五 dissent 3。

### 设计（5 项扩充）

1. **QuestDef（layer0 数据）**：id + name + giver + trigger + description + objective + reward。
2. **QuestObjective（S4 最小）**：kind=give_item + npc_id + item_id。
3. **QuestReward（S4 最小）**：exp + flag + message。
4. **QuestLog 组件**：玩家任务状态 `{quest_id: not_started | in_progress | completed}`。
5. **命令**：ask 接任务（quest trigger 优先于 inquiry）、give 完成任务、quest 查询。

### LPC 规格对照

对照 `d/xueshan/npc/gelun1.c` ask 还愿 + accept_object 酥油罐 + set marks/酥，抽象为：
- trigger=还愿 -> 接任务
- objective=give_item(npc=gelun1, item=suyou_guan)
- reward=exp + flag=酥 + message

### 完整任务闭环（kill criteria 1 验证）

ask 还愿 -> status in_progress -> give 酥油 -> status completed + exp + flag 酥 -> go north 放行。DSL 能表达"接任务 -> 完成目标 -> 获得奖励 -> 状态变更影响世界规则"闭环，**无逃生舱**。

### 结果

- 83 tests 全绿（+8：`test_quest.py` 6 + xueshan e2e 2），ruff 全过
- 阶段 -1 kill criteria 1 的"1 任务 + 1 对话全 DSL"验证通过
- 度量脚本 L1-L4 全绿，结构错误 0

### 范围边界（不做）

不做 kill_npc/reach_room 目标、物品/金钱奖励、任务链/日常/限时/共享、完整 job_system 迁移（均后置阶段 0 / M2）；不修改 LPC 源。

## 十二、S4d/S4e 实施记录（SchemaValidator 四道校验 + 扩展到 8 房间全量验证）

> S4d 完成（92 tests）/ S4e 完成（99 tests）。S4d 详情见 [ADR-0008](../../docs/adr/ADR-0008-schema-validator-four-checks.md)。

### S4d：SchemaValidator 四道校验

- `SceneValidator` 四道校验：SchemaValidator（pydantic strict + 未知字段警告）/ CapabilityAuditor / ResourceBudgetChecker / DependencyResolver
- 阶段 -1 最小实现，warning 不阻塞编译；measure_revision L2 集成
- 详情见 [ADR-0008](../../docs/adr/ADR-0008-schema-validator-four-checks.md)

### S4e：扩展到 8 房间全量验证（kill criteria 1 全量）

- xueshan_micro 3->8 房间（+ frontyard/yanwu/zoulang/jingang/chufang），对照 LPC 同名 .c；guangchang 加 north exit 接入扩展路径
- 第二 NPC xlama2（chufang，inquiry 酥油茶静态回复，对照 d/xueshan/npc/xlama2.c）
- GAP（后置）：xlama2 ask_tea 的 set_flag 茶 + accept_object 酥油的 clear_flag + 物品生成需 ask->action 机制 / clear_flag action / 物品系统（S4+/阶段 0）
- **阶段 -1 kill criteria 1 全量验证通过**（8 房间 + 2 NPC + 1 任务 + 2 对话全 DSL，无逃生舱）
- measure_revision L1-L4 + 四道校验全绿，结构错误 0；修订比例 75% 系规模扩展非 Agent 质量信号
- 99 tests 全绿（+7），ruff 全过

### 范围边界（不做）

不做 ask->action 机制 / clear_flag action / 物品生成 / 门状态机运行时（S4+/阶段 0）；不修改 LPC 源。

---

*创建：2026年7月10日*

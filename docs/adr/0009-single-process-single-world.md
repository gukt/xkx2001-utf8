---
Status: accepted
---

# 单机阶段约定：单进程单 World

当前引擎存在进程级全局注册表（如技能、门派、命令注册表）与 `World` 实例并存。M3 停机加固拍板（2026-07-21）：**单机阶段明确约定单进程只承载一个 `World`**；上述全局表与该假设绑定，不在本阶段迁入 World 实例作用域。若未来需要同进程多 World，再单独立项迁移注册表。动机：承认现状、避免在加固窗口做无调用者的作用域重构，并防止后人把「可测的单 World CLI」误读成已支持多世界隔离。

# 12 - 组件字段三态标注

**What to build:** `components.py` 补"瞬时（运行时可变不进存档）"第三态（避坑清单 §28,现有两态:运行时可变进存档 / 启动固定）,`save.py` 按三态过滤瞬时字段不进存档。为避坑清单 §37"短延迟内存态 vs 长周期持久态"分层铺路。Nature 衍生态（B 块）会是瞬时字段的第一个真实用例。

- **`components.py` 字段注释补第三态**"瞬时（运行时可变不进存档）"标注,与现有"运行时可变 vs 启动固定"两态并列。
- **`save.py` 序列化按三态决定**哪些字段进存档:瞬时字段不进存档。
- **成本极低**（注释 + save 字段过滤）,但为 §37 分层与 Nature 不存档提供标注依据。

**Blocked by:** None - 可立即开始（改 `components` + `save`,独立）。

**Status:** ready-for-agent

- [ ] `components.py` 字段注释有三态标注（运行时可变进存档 / 启动固定 / 瞬时运行时可变不进存档）
- [ ] `save.py` 按三态过滤:瞬时字段不进存档
- [ ] 挂一个瞬时字段的组件,存档后该字段不出现、恢复后缺失
- [ ] 现有组件的存档行为不变（不回归）

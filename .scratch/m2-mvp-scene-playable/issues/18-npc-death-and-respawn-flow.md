# 18 — NPC 死亡与重生：一次性消失 + 战利品掉落 + Spawner 重生

**What to build:** 落地 spec Implementation Decisions 用户故事 60（块 G）："NPC 死亡（气血耗尽）不走玩家的'昏迷->等待复活'流程，而是直接消失并掉落战利品（金钱/物品），复活/重生走 04 号票已修复的 spawner 注册表机制（如果该模板声明了 `respawn`）"。战斗 tick 路径中，若气血归零的一方**没有** `PlayerSession`（即 NPC），走本票的专属分支：不调用 06/17 号票的昏迷/死亡状态机，直接从 `World` 移除该 NPC 实体的可交互存在（是否物理删除实体或只是移除关键组件由实现阶段决定，需保证不再出现在 `look`/战斗匹配等任何查询路径中）；按 NPC 声明的战利品配置（金钱数额区间/掉落物品模板引用，纯数据）把物品放进 NPC 死亡房间地面、把金钱转移给击杀者（走 07 号票 `Currency`）；给击杀者经验奖励（对应 spec 用户故事 26）；若该 NPC 的 `template_key` 声明了 `respawn`，则不主动触发重生（04 号票的 `_spawn_scan` 会在下次周期扫描时自然发现缺口并补齐，本票不重复实现重生逻辑，只需保证"NPC 死亡后从存活实例集合中消失"这一前提成立，让 04 号票的机制能接得上）。

**Blocked by:** 04（spawner 蓝图注册表，NPC 消失后重生要接得上），07（`Currency`，金钱战利品），12（真实战斗伤害路径）。

**Status:** resolved

- [ ] 战斗 tick 结算路径按气血归零一方是否有 `PlayerSession` 分流：有 -> 06/17 号票流程；无 -> 本票流程。
- [ ] NPC 死亡：从"存活实例"查询语义上完全消失（`entities_with(NpcSpawnMeta)`/`entities_in_room` 等现有查询路径不应再返回它）；死亡房间地面出现掉落物品（如声明了掉落配置）。
- [ ] 战利品配置：纯数据字段（NPC YAML 声明，如 `loot: {currency: [10, 30], items: ["解毒丹"]}`，具体字段形状由实现阶段定，需能表达"金钱区间/固定值"与"物品模板引用列表"两类掉落），未声明掉落配置的 NPC 死亡不产出任何战利品（不是错误）。
- [ ] 击杀者获得经验奖励（写入击杀者某个技能的 `SkillProgress.exp`，具体"奖励哪个技能经验"的规则——如"当前交战用的招式所属技能"——需要明确并测试锁定）与金钱战利品（`Currency.amount` 增加）。
- [ ] 声明 `respawn=True` 的模板：验证 04 号票的 `_spawn_scan` 在下一次扫描周期确实能发现并补齐缺口（可以是集成测试：真实走一次战斗击杀流程后触发 tick 推进到扫描周期，断言新实例出现），不满足 `respawn` 的模板死亡后不重生。
- [ ] tick/命令层测试覆盖：无掉落配置的 NPC 死亡（无战利品，不报错）、有掉落配置的完整战利品流程、`respawn=True` 模板死亡后经扫描周期重生。
- [ ] 现有测试全绿不回归。

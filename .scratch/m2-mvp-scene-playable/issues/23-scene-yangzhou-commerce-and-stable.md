# 23 — 场景内容：扬州商业地标 + 马厩（buy/sell + 坐骑购买验收）

**What to build:** 落地 [10 号票](../../mvp-scope/issues/10-mvp-scenes-selection.md) 定稿的扬州商业地标（spec 用户故事 63–64、47、块 H）：客栈、钱庄、打铁铺、镖局、武庙、茶馆六个地标房间，每个房间有符合其功能的 NPC/描述；钱庄与打铁铺的 NPC 挂 `ShopInventory`，能实际 `buy`/`sell`（对应 07 号票机制的真实题材验收场，不是纯装饰房间）；新增马厩房间 + 马夫 NPC，`buy` 一匹坐骑（对应 10 号票"购买坐骑"机制的真实题材验收场）。房间键使用 `yangzhou_*` 前缀，与 22 号票共用命名空间（协调避免冲突）。

**Blocked by:** 07（`buy`/`sell`/`ShopInventory`），10（坐骑购买机制）。

**Status:** ready-for-agent

- [ ] 客栈/钱庄/打铁铺/镖局/武庙/茶馆六个地标房间，各有符合功能的描述与至少一个 NPC（不要求每个都能交易——武庙/茶馆/镖局/客栈可以是纯展示型 NPC + 剧情向描述，钱庄/打铁铺必须能交易）。
- [ ] 钱庄 NPC 挂 `ShopInventory`（如兑换/收购金钱相关物品，具体清单内容由实现阶段设计，需引用 `items:` 段已声明的物品模板并都带 `Valuable`）；`buy`/`sell` 在此房间对该 NPC 实际可用。
- [ ] 打铁铺 NPC 挂 `ShopInventory`（武器/装备类物品，同样需要 `Valuable`），`buy`/`sell` 实际可用。
- [ ] 马厩房间 + 马夫 NPC：`buy <坐骑>` 按 10 号票已定的购买机制成功获得一匹坐骑（出现在玩家当前房间，可立即 `ride`）。
- [ ] 与 22 号票的房间键不冲突（建议共同维护一份房间键清单，见 to-tickets-notes.md 的协调说明）；出口把商业地标接入 22 号票已建的扬州导航图（枢纽/大街可达）。
- [ ] 命令层测试覆盖：钱庄/打铁铺 buy/sell 完整路径、马厩购买坐骑后立即 `ride` 成功。
- [ ] 现有测试全绿不回归；`just verify-*` 一键矩阵全绿。
